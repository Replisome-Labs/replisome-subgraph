{
  "name": "higgspixel-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ higgspixel-subgraph",
    "create-local": "graph create --node http://localhost:8020/ higgspixel-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ higgspixel-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 higgspixel-subgraph",
    "test": "graph test",
    "coverage": "graph test -- -c"
  },
  "dependencies": {
    "@amxx/graphprotocol-utils": "^1.1.0",
    "@graphprotocol/graph-cli": "0.37.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
